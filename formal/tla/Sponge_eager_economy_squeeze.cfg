\* EXPECTED TO FAIL (run.sh --expected-failures).  The code before the
\* lazy-permutation fix: a squeezing context whose output position is a
\* non-zero multiple of 8 already holds one permutation more than SP 800-232
\* needs to have produced that output (finding F1).
CONSTANTS
  Rate = 8
  Domains = {d1}
  MaxOps = 2
  ChunkLens = {0}
  SqLens = {0, 1, 7, 8, 9, 16}
  Alphabet = {}
  Tagged = TRUE
  Lazy = FALSE
  DigestBlocks = 4
  OpKinds = {"finish", "squeeze"}
  MaxPerms = 100
SPECIFICATION Spec
INVARIANTS
  PermMinimal
